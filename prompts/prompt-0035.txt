You are a senior software engineer performing a clean-room migration of legacy UI to React + TypeScript.

CLEAN-ROOM & LICENSE (MANDATORY)
- Behavioral spec only. No source code, no pseudo-code, no decompiled content.
- Avoid private/internal identifiers, class names, namespaces, file-internal strings. Keep only business-level field names and public routes/contracts.
- Describe validation in words/JSON rules (e.g., “email format”, “YYYY-MM-DD”) — never copy regexes or code.
- If logic seems proprietary/ambiguous, add "proprietaryAlgorithmSuspected" in risks.
- In business/UX naming ONLY (entities, labels, route examples, UX notes), replace “OpenDental”/“Open Dental” with “PractX”. Do NOT alter file paths or third-party names.

SCOPE
- Process ONLY the artifacts listed in the batch JSON.
- For each artifact, OPEN the files listed under source.files (markup, designer, view, controller, code-behind) and EXTRACT “signals” (see Phase 1).
- Use those signals to produce a meaningful spec (Phase 2). If signals are insufficient, mark the artifact for manual review with a clear reason.

PHASE 1 — SIGNALS (per artifact)
From the listed files, derive safe, minimal cues (no code):
- labels: short visible labels/headers (≤ 6 words each)
- gridColumns: column headers or obvious table headings
- fieldHints: obvious domain fields implied by labels/placeholders (e.g., patientId, firstName, dateOfBirth)
- validationHints: words like “required”, “email”, “date”, “numeric”, “max length”
- lookupHints: presence of list/lookup controls (e.g., dropdowns with value/label)
- intentHints: button/menu verbs (Save, Cancel, Verify, Add, Remove, Upload, Print)
- platformHints: printing, file dialogs, serial/USB, ViewState/PostBack, long-running/background worker
- counts: {labels, gridColumns, buttons/verbs}
Record which files contributed to each hint (filenames only). Do not include code or long text.

PHASE 2 — SPEC (per artifact)
Use Phase 1 signals to populate these sections with business-level content:
- entities: at least 1 entity if any fieldHints exist
- form.fields: at least 3 fields when ≥3 relevant hints exist (otherwise include what’s supported)
- grids: include each table with ≥1 column label
- intents: include verbs/actions found
- validation.rules: include inferred cross-field or format rules from hints
- platform.desktopOnly + webAlternatives: if platformHints indicate desktop-only needs
- risks: include red flags (threading, ViewState, server round-trips, complex server events, client scripts, proprietaryAlgorithmSuspected)

MINIMUM CONTENT & EVIDENCE (per artifact)
- At least ONE of: entities | form.fields | grids | intents must be NON-EMPTY, else mark:
  "needsManualReview": true and "reviewReason": "insufficientSignals"
- Include "evidence": { "fromFiles": ["..."], "signalCounts": { ... } } so we can audit coverage.

INPUT
- Open and use: batches/batch-0035.json

OUTPUT: ONE valid JSON object only:
{
  "batch": { "index": 35, "totalBatches": 42 },
  "specs": [
    {
      "source": {
        "sourceType": "WinForms | WPF | ASPX | MVC | RazorPages | BlazorServer | BlazorWasm | UWP | WinUI | XamarinForms | MAUI | AngularJS | Knockout | Silverlight | Other",
        "qualifiedName": "TypeNameOrRoute (omit private/internal identifiers if not public)",
        "project": "ProjectName",
        "files": [{"path":"relative/or/absolute/path","role":"markup|codeBehind|controller|view|designer|viewModel|other"}],
        "metrics": { "approxLines": 0, "eventHandlers": 0 }
      },
      "route": { "pattern": "/entity/{id}/edit", "params": [], "queryParams": [] },
      "entities": {},
      "form": { "fields": [], "lookups": {}, "validation": { "rules": [] } },
      "grids": [],
      "intents": [],
      "api": [],
      "navigation": { "linksOut": [], "breadcrumbs": [] },
      "auth": { "requiresAuth": true, "roles": [], "claims": [], "sensitiveFields": [] },
      "platform": { "desktopOnly": [], "webAlternatives": {} },
      "sideEffects": [],
      "risks": [],
      "uxNotes": [],
      "evidence": {
        "fromFiles": [],
        "signalCounts": { "labels": 0, "gridColumns": 0, "fieldHints": 0, "intentHints": 0 }
      },
      "needsManualReview": false,
      "reviewReason": ""
    }
  ],
  "suggestedFileName": "spec-0035.json"
}

RULES
- Return ONLY the JSON object. No prose.
- If unknown, emit empty arrays/objects (not null).
- Do not exceed the artifacts in the batch.
